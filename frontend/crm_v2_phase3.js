(()=>{const base=localStorage.getItem('backendApiUrl')||((location.hostname==='localhost'||location.hostname==='127.0.0.1'||location.protocol==='file:')?'http://localhost:5000/api':location.origin+'/api');const app=document.getElementById('app');const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const money=n=>'₹'+Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:0});async function get(p){const r=await fetch(base+p,{cache:'no-store'});const j=await r.json();if(!r.ok||!j.success)throw Error(j.message||'Request failed');return j.data}function k(t,v){return `<div class="card"><div class="k">${esc(t)}</div><div class="v">${money(v)}</div></div>`}async function dashboard(){const [pipe,age,prof]=await Promise.all([get('/crm/v2/pipeline'),get('/crm/v2/ageing'),get('/crm/v2/profitability')]);app.innerHTML=`<div class="grid">${k('Pipeline',pipe.total)}${k('Weighted Pipeline',pipe.weighted)}${k('Receivable',age.total)}${k('Sales',prof.sales)}${k('Gross Profit',prof.profit)}<div class="card"><div class="k">Margin</div><div class="score">${prof.margin.toFixed(1)}%</div></div></div><div class="two"><div class="card"><h3>Pipeline Stages</h3>${pipe.stages.map(x=>`<p><b>${esc(x.status)}</b> · ${x.count} · ${money(x.value)}<div class="bar"><i style="width:${pipe.total?Math.min(100,x.value/pipe.total*100):0}%"></i></div></p>`).join('')||'No pipeline data'}</div><div class="card"><h3>Receivable Ageing</h3>${Object.entries(age.buckets).map(([b,v])=>`<p><b>${esc(b)}</b> — ${money(v)}</p>`).join('')}</div></div>`}async function pipeline(){const d=await get('/crm/v2/pipeline');app.innerHTML=`<div class="card"><h2>Sales Pipeline</h2><table class="table"><tr><th>Stage</th><th>Count</th><th>Value</th><th>Weighted</th></tr>${d.stages.map(x=>`<tr><td>${esc(x.status)}</td><td>${x.count}</td><td>${money(x.value)}</td><td>${money(x.weightedValue)}</td></tr>`).join('')}</table><p><b>Total:</b> ${money(d.total)} &nbsp; <b>Weighted:</b> ${money(d.weighted)} &nbsp; <b>Win Rate:</b> ${d.winRate||0}%</p></div>`}async function ageing(){const d=await get('/crm/v2/ageing');app.innerHTML=`<div class="grid">${Object.entries(d.buckets).map(([b,v])=>k(b,v)).join('')}</div><div class="card"><h2>Outstanding Invoices</h2><table class="table"><tr><th>Invoice</th><th>Customer</th><th>Due</th><th>Bucket</th><th>Balance</th></tr>${d.rows.map(x=>`<tr><td>${esc(x.invoiceNo||x.id)}</td><td>${esc(x.customerName||'-')}</td><td>${esc(x.dueDate||'-')}</td><td><span class="pill">${esc(x.bucket)}</span></td><td>${money(x.balance)}</td></tr>`).join('')}</table></div>`}async function profit(){const d=await get('/crm/v2/profitability');app.innerHTML=`<div class="grid">${k('Sales',d.sales)}${k('Cost',d.cost)}${k('Gross Profit',d.profit)}</div><div class="card"><h2>Profitability by Customer</h2><table class="table"><tr><th>Customer</th><th>Sales</th><th>Cost</th><th>Profit</th></tr>${d.byCustomer.map(x=>`<tr><td>${esc(x.customer)}</td><td>${money(x.sales)}</td><td>${money(x.cost)}</td><td>${money(x.profit)}</td></tr>`).join('')}</table></div>`}async function approvals(){const d=await get('/crm/v2/approvals');app.innerHTML=`<div class="card"><h2>Approval Queue</h2><p class="muted">Use this queue for discount, quotation, purchase and other controlled approvals.</p><table class="table"><tr><th>Module</th><th>Reference</th><th>Amount</th><th>Requested By</th><th>Status</th></tr>${d.map(x=>`<tr><td>${esc(x.module)}</td><td>${esc(x.recordRef||x.recordId)}</td><td>${money(x.amount)}</td><td>${esc(x.requestedBy)}</td><td><span class="pill">${esc(x.status)}</span></td></tr>`).join('')||'<tr><td colspan="5">No approvals.</td></tr>'}</table></div>`}async function lookup(){app.innerHTML=`<div class="card"><h2>Customer / Supplier 360°</h2><div class="searchrow"><select id="typ"><option value="customer">Customer</option><option value="supplier">Supplier</option></select><input id="id" placeholder="Enter internal Customer/Supplier ID"><button class="btn primary" id="go">Open 360°</button></div><div id="out" style="margin-top:14px"></div></div>`;document.getElementById('go').onclick=async()=>{const type=document.getElementById('typ').value,id=document.getElementById('id').value.trim();if(!id)return;const d=await get(`/crm/v2/360/${type}/${encodeURIComponent(id)}`);const x=d.kpis;document.getElementById('out').innerHTML=`<div class="grid">${type==='customer'?`${k('Sales',x.sales)}${k('Receivable',x.receivable)}${k('Quotation Value',x.quoteValue)}`:`${k('Purchase Value',x.purchaseValue)}${k('PO Count',x.poCount)}${k('On-Time %',x.onTimeRate)}`}</div><div class="card"><b>${esc(d[type].name||d[type].customerName||'')}</b><p class="muted">Health / performance: ${x.healthScore??x.qualityScore??'-'}</p></div>`}}async function roles(){const d=await get('/crm/v2/roles');app.innerHTML=`<div class="card"><h2>Role & Permission Matrix</h2><table class="table"><tr><th>Role</th><th>Active</th><th>Permissions</th></tr>${d.map(r=>`<tr><td>${esc(r.name)}</td><td>${r.active?'Yes':'No'}</td><td>${esc(Object.values(r.permissions||{}).join(', ')||'-')}</td></tr>`).join('')}</table></div>`}async function load(tab){try{if(tab==='dashboard')return dashboard();if(tab==='pipeline')return pipeline();if(tab==='ageing')return ageing();if(tab==='profit')return profit();if(tab==='approvals')return approvals();if(tab==='360')return lookup();if(tab==='roles')return roles()}catch(e){app.innerHTML=`<div class="card" style="color:#b42318">${esc(e.message)}</div>`}}document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');load(b.dataset.tab)});load('dashboard')})();