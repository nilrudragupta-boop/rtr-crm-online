(()=>{const base=localStorage.getItem('backendApiUrl')||((location.hostname==='localhost'||location.hostname==='127.0.0.1'||location.protocol==='file:')?'http://localhost:5000/api':location.origin+'/api');const app=document.getElementById('app');const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const money=n=>'₹'+Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:0});async function get(p){const r=await fetch(base+p,{cache:'no-store'});const j=await r.json();if(!r.ok||!j.success)throw Error(j.message||'Request failed');return j.data}async function post(p,b){const r=await fetch(base+p,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const j=await r.json();if(!r.ok||!j.success)throw Error(j.message||'Request failed');return j.data}
function card(t,v){return `<div class="card"><div class="k">${esc(t)}</div><div class="v">${v}</div></div>`}function err(e){app.innerHTML=`<div class="card bad">${esc(e.message)}</div>`}
async function dashboard(){const [n,r]=await Promise.all([get('/crm/v2/phase4/notifications'),get('/crm/v2/phase4/rules')]);app.innerHTML=`<div class="grid">${card('Open Automation Tasks',n.activities.length)}${card('Pending Approvals',n.approvals.length)}${card('Enabled Rules',r.filter(x=>x.enabled).length)}${card('Total Rules',r.length)}</div><div class="two"><div class="card"><h2>Automation Queue</h2><p class="muted">Phase 4 converts business conditions into actionable CRM tasks. Use <b>Run Automation</b> after data changes or keep your normal operational review cycle.</p><button class="btn primary" id="run2">Run Automation Now</button><div style="margin-top:14px">${n.activities.slice(0,8).map(x=>`<p><b>${esc(x.subject)}</b><br><span class="muted">${esc(x.details||'')} · ${esc(x.priority||'Medium')}</span></p>`).join('')||'<div class="empty">No automation tasks.</div>'}</div></div><div class="card"><h2>Phase 4 Operating Model</h2><p>1. Detect overdue / missing workflow conditions.</p><p>2. Create idempotent follow-up tasks.</p><p>3. Surface high-priority work centrally.</p><p>4. Monitor service SLA risk.</p><p>5. Tune thresholds by workflow rule.</p></div></div>`;document.getElementById('run2').onclick=runAutomation}
async function runAutomation(){const b=document.getElementById('run');b.disabled=true;try{const d=await post('/crm/v2/phase4/run',{});alert(`Automation complete: ${d.created} task(s) created, ${d.skipped} already handled.`);await load('dashboard')}catch(e){err(e)}finally{b.disabled=false}}
async function notifications(){const d=await get('/crm/v2/phase4/notifications');app.innerHTML=`<div class="card"><h2>Notification Centre</h2><table class="table"><tr><th>Priority</th><th>Task</th><th>Related</th><th>Status</th><th>Due</th></tr>${d.activities.map(x=>`<tr><td><span class="pill ${x.priority==='High'?'bad':''}">${esc(x.priority)}</span></td><td><b>${esc(x.subject)}</b><br><span class="muted">${esc(x.details||'')}</span></td><td>${esc(x.relatedModule||'-')} · ${esc(x.relatedId||'-')}</td><td>${esc(x.status)}</td><td>${esc(x.dueDate||'-')}</td></tr>`).join('')||'<tr><td colspan="5">No notifications.</td></tr>'}</table></div><div class="card" style="margin-top:14px"><h2>Pending Approvals</h2><table class="table"><tr><th>Module</th><th>Reference</th><th>Amount</th><th>Requested By</th></tr>${d.approvals.map(x=>`<tr><td>${esc(x.module)}</td><td>${esc(x.recordRef||x.recordId)}</td><td>${money(x.amount)}</td><td>${esc(x.requestedBy)}</td></tr>`).join('')||'<tr><td colspan="4">No pending approvals.</td></tr>'}</table></div>`}
async function sla(){const d=await get('/crm/v2/phase4/sla');app.innerHTML=`<div class="grid">${card('Within SLA',d.summary.within)}${card('At Risk',d.summary.atRisk)}${card('Breached',d.summary.breached)}${card('Open Tickets',d.tickets.length)}</div><div class="card"><h2>Service SLA Monitor</h2><table class="table"><tr><th>Ticket</th><th>Status</th><th>Age</th><th>SLA</th><th>Condition</th></tr>${d.tickets.map(x=>`<tr><td>${esc(x.ticketNo||x.id)}</td><td>${esc(x.status||'-')}</td><td>${x.ageDays} day(s)</td><td>${x.slaDays} day(s)</td><td class="${x.slaStatus==='Breached'?'bad':x.slaStatus==='At Risk'?'warn':'good'}"><b>${esc(x.slaStatus)}</b></td></tr>`).join('')||'<tr><td colspan="5">No open service tickets found.</td></tr>'}</table></div>`}
async function rules(){const d=await get('/crm/v2/phase4/rules');app.innerHTML=`<div class="card"><h2>Workflow Rules</h2><p class="muted">These rules define when Phase 4 creates operational tasks. Changes are non-destructive and can be disabled.</p>${d.map(x=>`<div class="rule"><div><b>${esc(x.name)}</b><div class="muted">${esc(x.description||x.trigger)}</div></div><div>${esc(x.trigger)}</div><div>${x.thresholdDays||0} day(s)</div><div><span class="pill">${x.enabled?'Enabled':'Disabled'}</span></div><button class="btn switch" data-id="${esc(x.id)}" data-enabled="${x.enabled}">${x.enabled?'Disable':'Enable'}</button></div>`).join('')}</div>`;document.querySelectorAll('.switch').forEach(b=>b.onclick=async()=>{try{await post('/crm/v2/phase4/rules',{id:b.dataset.id,enabled:b.dataset.enabled!=='true'});rules()}catch(e){err(e)}})}
async function runs(){const d=await get('/crm/v2/phase4/notifications');const a=await get('/crm/v2/activities?limit=200');app.innerHTML=`<div class="card"><h2>Automation Activity</h2><p class="muted">Generated tasks are stored in the universal activity layer for traceability.</p><table class="table"><tr><th>Subject</th><th>Module</th><th>Reference</th><th>Created</th></tr>${a.filter(x=>x.automation).map(x=>`<tr><td>${esc(x.subject)}</td><td>${esc(x.relatedModule||'-')}</td><td>${esc(x.relatedId||'-')}</td><td>${esc(x.createdAt||'-')}</td></tr>`).join('')||'<tr><td colspan="4">No automation runs recorded.</td></tr>'}</table></div>`}
async function load(t){try{if(t==='dashboard')return dashboard();if(t==='notifications')return notifications();if(t==='sla')return sla();if(t==='rules')return rules();if(t==='runs')return runs()}catch(e){err(e)}}document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');load(b.dataset.tab)});document.getElementById('run').onclick=runAutomation;load('dashboard')})();